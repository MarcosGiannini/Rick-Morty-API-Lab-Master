// src/scenes/character.scene.tsx

import * as React from 'react';
import { CharacterContainer } from '@/pods/character';

// Aquí también quitamos el AppLayout.
export const CharacterScene: React.FC = () => (
  <CharacterContainer />
);