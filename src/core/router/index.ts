// src/core/router/index.ts

/**
 * @description Punto de entrada (barrel file) para los módulos del enrutador.
 * Exporta el componente principal del router y las definiciones de rutas,
 * facilitando las importaciones desde otros módulos de la aplicación.
 */
export * from './router.component'; // Exporta el componente Router.
export * from './routes'; // Exporta el objeto appRoutes.